rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Android accesses the gateway only. Server service-account IAM controls access.
    match /{document=**} { allow read, write: if false; }
  }
}
